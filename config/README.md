# config/

YAML configuration files for runtime behavior: `bu_registry.yaml` (list of BUs and their owners), `bu_profiles.yaml` (per-BU product interests and notification preferences), and `policy.yaml` (gate thresholds, hold rules, digest schedules). Authored in prompt 03.
