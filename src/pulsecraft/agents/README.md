# src/pulsecraft/agents/

Python wrappers for invoking the three Claude specialist subagents (SignalScribe, BUAtlas, PushPilot) via the Claude Agent SDK. Each wrapper handles prompt loading, input marshalling, output parsing, and retry logic. Authored in prompts 05–07.
