# src/pulsecraft/

The main Python package for PulseCraft. Contains the orchestrator, agent invocation wrappers, skill implementations, Pydantic schemas, CLI entry points, and observability utilities. Each sub-package is authored in a dedicated build prompt.
