# .claude/commands/

Claude Code slash command definitions (e.g., `/ingest`, `/approve`, `/status`). These wrap the CLI entry points for use directly inside Claude Code sessions. Authored in prompt 11.
