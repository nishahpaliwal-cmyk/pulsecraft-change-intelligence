# Prompt 16.1 — Architecture Tab Surgical Fixes

> **Character.** Four specific fixes to the Architecture tab shipped in prompt 16. Keep everything that works (node design, detail panel content, interactions, animation). Fix only the four items below.
>
> **Expected duration:** 60-90 minutes.
>
> **Budget:** Zero LLM cost.

---

## Fix 1 — Remove the "HITL eval" pseudo-node

HITL routing is a *behavior* of the `pre_deliver` hook — not a separate stage. Remove the hitl_eval node and its edges. Reroute: `buatlas → pushpilot` directly. Update `pre_deliver` detail panel to explain HITL routing as its primary enforcement behavior.

After fix, pipeline reads: `Ingest → SignalScribe → post_agent → BUAtlas → PushPilot → pre_deliver → Terminal state`.

## Fix 2 — Add fan-out visualization on BUAtlas

Render two ghost rectangles behind the BUAtlas node (offsets +4px and +8px, opacity 0.42 and 0.22) using the teal accent color. Ghost cards are `pointer-events: none`. Update sub-label to `Gates 4 · 5 · asyncio fan-out`. Parallelism should read at a glance without reading the label.

## Fix 3 — Add agent-vs-code principle callout

Add below the detail panel:

> *Agents express preferences based on context. Code enforces invariants based on policy. When they diverge, policy wins — and both are logged.*
>
> The pre_deliver hook is the primary site of this divergence. Three agents reason about what should happen; guardrail hooks enforce what is allowed to happen. Both judgments are captured in the audit trail, enabling ongoing calibration between agent confidence and policy thresholds.

Container: pale cream `#F8F3E8`, 1px border, 12px radius, 28px/36px padding. Principle in Fraunces italic 500 20px. Explanation in Inter 400 15px.

## Fix 4 — Reposition replay button

Move from bottom of page into `#arch-canvas` as `position: absolute; top: 16px; right: 16px`. Add `position: relative` to `.arch-canvas`. White background, `#C9C1B0` border.

---

## Outcome (session 16.1 — 2026-04-24)

Commit: `920a745`

**Fix 1:** `hitl_eval` node removed. All 3 edges involving it removed. Direct `buatlas → pushpilot` edge added. `terminal` repositioned to `cx=1000, cy=190` (directly below `pre_deliver`), `SVG_H` reduced from 340 to 240. `pre_deliver` detail panel body rewritten to lead with HITL routing as first enforcement rule; `pills` updated to include `'HITL routing'`.

**Fix 2:** BUAtlas `_drawNode` special-cases `node.id === 'buatlas'` to prepend two ghost `<rect>` pairs (main rect + accent bar) at `{off:8, op:0.22}` and `{off:4, op:0.42}` before the shadow/main rect — SVG stacking puts them behind. Sub-label updated to `'Gates 4 · 5 · asyncio fan-out'`. The stacked-card depth effect reads clearly at a glance.

**Fix 3:** `_buildCallout()` function adds `.arch-callout` div after detail panel. `.arch-callout__principle` (Fraunces italic 500 20px) + `.arch-callout__explanation` (Inter 400 15px). CSS in `architecture.css`.

**Fix 4:** `_buildReplayBtn()` appends to `document.getElementById('arch-canvas')` instead of root. `.arch-canvas` gains `position: relative`. `.arch-replay-btn` is now `position: absolute; top: 16px; right: 16px; z-index: 1`.

**Test count:** 642 passed (unchanged).
