# schemas/

JSON Schema files (`.json`) that define the wire format for data contracts at agent boundaries. Used for validation at judgment gates. Authored in prompt 02, alongside the Pydantic models in `src/pulsecraft/schemas/`.
