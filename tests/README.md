# tests/

Test suite for PulseCraft. Organized into `unit/` (pure logic, no I/O), `integration/` (orchestrator + agent flows against fixture data), and `fixtures/` (shared test data and factories). Run with `pytest`.
